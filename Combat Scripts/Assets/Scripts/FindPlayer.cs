using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FindPlayer : MonoBehaviour
{
/*
    public Unit ClosestPlayer(List<GridStats> grid)
    {
        GridStats closest = new GridStats();
        for (int i = 0; i <= grid.Count; i++)
        {
            if (grid[i].GetComponent<Unit>())
            {
                if (i == 0)
                {
                    closest = grid[i];
                }
                else if (grid[i].visited < closest.visited)
                {
                    closest = grid[i];
                }
            }
        }
        return closest.unit;
    }
*/
}
